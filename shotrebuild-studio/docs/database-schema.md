# SQLite Schema（MVP）

初始化文件：`backend/db/schema.py`

已创建表：
- projects
- assets
- providers
- provider_models
- workflows
- provider_jobs
- qa_results
- library_items
- system_events

说明：
- 字段先满足结构清晰与最小可用。
- `provider_jobs` / `qa_results` / `library_items` 当前用于后续流程预留。
